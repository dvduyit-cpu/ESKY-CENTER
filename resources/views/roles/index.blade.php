@extends('layouts.app')
@section('title','Vai trò & quyền') @section('header','Vai trò và phân quyền')
@section('content')
<div class="d-flex flex-wrap justify-content-between align-items-center gap-3 mb-4"><div><h1 class="page-title">Vai trò & quyền</h1><div class="page-subtitle">Quyền theo vai trò được dùng mặc định; Admin có thể ghi đè cho từng tài khoản.</div></div>@if(auth()->user()->allowed('roles','create'))<a class="btn btn-primary" href="{{ route('roles.create') }}"><i class="bi bi-plus-circle me-2"></i>Thêm vai trò</a>@endif</div>
@if(auth()->user()->allowed('roles','delete'))<form id="bulk-roles" method="POST" action="{{ route('roles.bulk-destroy') }}" data-bulk-form="roles" data-bulk-confirm="Xóa vĩnh viễn các vai trò đã chọn?" class="mb-3">@csrf @method('DELETE')<label class="me-3"><input class="form-check-input me-1" type="checkbox" data-bulk-all="roles"> Chọn tất cả có thể xóa</label><button class="btn btn-sm btn-outline-danger" data-bulk-submit disabled><i class="bi bi-trash me-1"></i>Xóa vĩnh viễn (<span data-bulk-count>0</span>)</button></form>@endif
<div class="row g-4">@foreach($roles as $r)<div class="col-md-6 col-xl-4"><div class="card card-soft h-100"><div class="card-body p-4"><div class="d-flex justify-content-between align-items-start"><div class="stat-icon bg-primary-subtle text-primary"><i class="bi bi-shield-check"></i></div>@if($r->is_system)<span class="badge-soft badge-info">Hệ thống</span>@else<span class="badge-soft badge-gray">Tùy chỉnh</span>@endif</div><h4 class="mt-4 mb-1">{{ $r->name }}</h4><div class="text-muted small">{{ $r->code }}</div><p class="text-muted mt-3">{{ $r->description ?: 'Chưa có mô tả.' }}</p><div class="d-flex justify-content-between align-items-center mt-4"><span><strong>{{ $r->users_count }}</strong> tài khoản</span><div>@if(auth()->user()->allowed('roles','update'))<a class="btn btn-sm btn-outline-primary" href="{{ route('roles.edit',$r) }}">Cấu hình quyền</a>@endif @if(!$r->is_system && auth()->user()->allowed('roles','delete'))<form class="d-inline" method="POST" action="{{ route('roles.destroy',$r) }}">@csrf @method('DELETE')<button class="btn btn-sm btn-outline-danger" data-confirm="Xóa vai trò này?"><i class="bi bi-trash"></i></button></form>@endif</div></div></div></div></div>@endforeach</div>
@if($roles->hasPages())<div class="mt-4">{{ $roles->links() }}</div>@endif
@endsection
