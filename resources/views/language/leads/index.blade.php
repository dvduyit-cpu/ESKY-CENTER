@extends('layouts.app') @section('title','Học viên tiềm năng') @section('header','Tư vấn tuyển sinh') @section('content')
@php($labels=['new'=>'Mới tiếp nhận','contacted'=>'Đã liên hệ','consulting'=>'Đang tư vấn','placement_test'=>'Hẹn kiểm tra','waiting'=>'Chờ phản hồi','registered'=>'Đã đăng ký','not_interested'=>'Không quan tâm','follow_up'=>'Chăm sóc lại'])
<div class="d-flex flex-wrap justify-content-between gap-3 mb-4"><div><h1 class="page-title">Học viên tiềm năng</h1><div class="page-subtitle">Theo dõi ngày nhập, tư vấn, CTV giới thiệu và chuyển đổi thành học viên chính thức.</div></div><div class="d-flex gap-2"><a class="btn btn-outline-success" href="{{route('language-leads.export')}}"><i class="bi bi-file-earmark-excel me-2"></i>Excel</a><a class="btn btn-primary" href="{{route('language-leads.create')}}"><i class="bi bi-plus-circle me-2"></i>Thêm học viên tiềm năng</a></div></div>
<form class="filter-panel row g-3 mb-4"><div class="col-lg-3"><input class="form-control" name="q" value="{{request('q')}}" placeholder="Mã, tên hoặc số điện thoại"></div><div class="col-lg-2"><select class="form-select" name="status"><option value="">Mọi trạng thái</option>@foreach($labels as $key=>$label)<option value="{{$key}}" @selected(request('status')===$key)>{{$label}}</option>@endforeach</select></div><div class="col-lg-2"><input class="form-control" type="date" name="date" value="{{request('date')}}" title="Lọc theo ngày nhập cụ thể"></div><div class="col-lg-2"><select class="form-select" name="month"><option value="">Mọi tháng</option>@for($month=1;$month<=12;$month++)<option value="{{$month}}" @selected(request('month')==$month)>Tháng {{$month}}</option>@endfor</select></div><div class="col-lg-2"><input class="form-control" type="number" name="year" value="{{request('year')}}" min="2020" max="2100" placeholder="Năm"></div><div class="col-lg-1"><button class="btn btn-dark w-100"><i class="bi bi-filter"></i></button></div></form>
<div class="card card-soft"><div class="table-responsive"><table class="table table-modern"><thead><tr><th>Khách hàng</th><th>Ngày nhập</th><th>Liên hệ</th><th>Khóa học</th><th>CTV / Tư vấn viên</th><th>Trạng thái</th><th></th></tr></thead><tbody>@forelse($items as $item)<tr><td><strong>{{$item->name}}</strong><div class="small text-muted">{{$item->code}}</div></td><td>{{$item->received_at?->format('d/m/Y')?:$item->created_at?->format('d/m/Y')}}</td><td>{{$item->phone}}<div class="small text-muted">{{$item->email}}</div></td><td>{{$item->course?->name?:'—'}}</td><td>{{$item->collaborator?->name?:'Không có CTV'}}<div class="small text-muted">TV: {{$item->consultant?->name?:'Chưa phân công'}}</div></td><td><span class="badge-soft badge-info">{{$labels[$item->status]??$item->status}}</span></td><td class="text-end text-nowrap"><a class="btn btn-sm btn-outline-secondary" href="{{route('language-leads.show',$item)}}" title="Xem khách hàng"><i class="bi bi-eye"></i></a> @if($item->status==='registered' && !$item->converted_student_id)<form class="d-inline" method="POST" action="{{route('language-leads.convert',$item)}}">@csrf<button class="btn btn-sm btn-outline-success" title="Chuyển thành học viên" data-confirm="Tạo học viên và chuyển sang lập học phí?"><i class="bi bi-person-check"></i></button></form>@endif <a class="btn btn-sm btn-outline-primary" href="{{route('language-leads.edit',$item)}}"><i class="bi bi-pencil"></i></a> <form class="d-inline" method="POST" action="{{route('language-leads.destroy',$item)}}">@csrf @method('DELETE')<button class="btn btn-sm btn-outline-danger" data-confirm="Xóa khách hàng này?"><i class="bi bi-trash"></i></button></form></td></tr>@empty<tr><td colspan="7"><div class="empty-state">Không có khách hàng phù hợp bộ lọc.</div></td></tr>@endforelse</tbody></table></div><div class="card-footer bg-white border-0">{{$items->links()}}</div></div>
@endsection
